# arize-ax-cli 0.32.0 pins arize==8.49.0 and pip resolves the pair in one step.
#
# Two earlier pins broke without this repo changing a line: the server began
# returning a field the pinned SDK's generated model did not know, and the model
# rejected the whole response. 8.43.1's Experiment refused the server's space_id;
# 8.44.0's TaskEvaluator refused its evaluator_version_id, which failed the scan
# *after* the evaluation task had already been created server-side. 8.49.0 knows
# both fields. tests/test_arize_sdk_contract.py asserts that and explains the
# failure shape, so the next drift is recognisable rather than mysterious.
arize-ax-cli==0.32.0
pyyaml>=6.0
pytest>=8.0

# The improvement loop's task runs the skill under test, so the task process
# calls the Anthropic API directly; the static scan does not and never has.
anthropic>=0.93.0
